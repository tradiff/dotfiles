- link:
    ~/.zshrc: .zshrc
    ~/zshrc: zshrc
    ~/.p10k.zsh: .p10k.zsh
    ~/.tmux.conf: .tmux.conf
    ~/.config/tmuxinator: tmuxinator
    ~/.config/nvim: nvim
    ~/.gitconfig: .gitconfig
    ~/.gitignore_global: .gitignore_global
    ~/.config/kitty: kitty
    ~/.config/starship.toml: starship.toml
    ~/.config/wezterm: wezterm
    ~/.config/alacritty: alacritty
    ~/.config/karabiner/karabiner.json:
      if: "uname | grep -qx Darwin"
      path: karabiner/karabiner.json
    ~/.config/yabai/yabairc:
      if: "uname | grep -qx Darwin"
      path: yabai/yabairc
    ~/.config/skhd/skhdrc:
      if: "uname | grep -qx Darwin"
      path: skhd/skhdrc
    ~/.config/hypr:
      if: "uname | grep -qx Linux"
      path: hypr
    ~/.config/rofi:
      if: "uname | grep -qx Linux"
      path: rofi
    ~/.config/dunst:
      if: "uname | grep -qx Linux"
      path: dunst
    ~/.config/waybar:
      if: "uname | grep -qx Linux"
      path: waybar
    ~/.config/awesome:
      if: "uname | grep -qx Linux"
      path: awesome
    ~/.xprofile:
      if: "uname | grep -qx Linux"
      path: xorg/.xprofile
    ~/bin:
      if: "uname | grep -qx Linux"
      path: bin
    ~/.config/espanso:
      if: "uname | grep -qx Linux"
      path: espanso
    ~/.config/wireplumber:
      if: "uname | grep -qx Linux"
      path: wireplumber
    ~/.config/zellij:
      path: zellij
    ~/.config/ruby:
      path: ruby
    ~/.config/wlogout: wlogout
    ~/.config/fontconfig: fontconfig
    ~/.config/ghostty: ghostty
    ~/.config/plasma-workspace/env: plasma-workspace/env
    ~/.psqlrc: .psqlrc
    ~/.config/atuin/config.toml: atuin/config.toml
    ~/.local/share/aurorae/themes/catppuccin-travis: aurorae/themes/catppuccin-travis
    ~/.config/fish: fish

- shell:
    - description: Switch to fish
      command: "echo $SHELL | grep $(which fish) || sudo chsh -s $(which fish) $(whoami)"
